#include <stdio.h>

int main() {
    int i;

    for (i = 5; i <= 10; i++) { // Inicializa i en 1, ejecuta el bucle mientras i sea menor o igual a 10, e incrementa i en 1 en cada iteración
        printf("%d\n", i); // Imprime el valor de i
    }

    return 0;
}
