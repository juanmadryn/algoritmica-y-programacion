/* Llamamos a la librería estándar "stdio" que contiene las funciones que 
	necesitamos: en este caso printf(): imprimir con formato */
#include <stdio.h>

// Definimos la función 'main' (programa pincipal)
int main() {

	// Imprimimos en pantalla el mensaje
    printf(">>> ¡Hola, mundo!\n");	
    
    /* Por convención, un código de retorno de cero de la función 'main' 
				   significa que el programa se completó correctamente*/
	return 0;
}
